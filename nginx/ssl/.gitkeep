# SSL certificates directory

Place your SSL certificates here:

- `cert.pem` - SSL certificate
- `key.pem` - Private key

For Let's Encrypt, certbot will handle this automatically.

This directory is git-ignored for security.
